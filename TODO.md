# TODO: mongodb-contact-crud Checkpoint

## Steps:
- [x] Implement CRUD in `contact.js` per checkpoint requirements
- [x] Update `README.md` instructions
- [ ] Replace placeholder files in `screenshots/` with real screenshots from `mongosh`/terminal/Compass
